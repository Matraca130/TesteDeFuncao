// ============================================================
// Axon v4.2 — AI Routes (Dev 6 stub)
// ============================================================
// Placeholder until Dev 6 delivers full AI routes
// (generate, approve, chat, keyword-popup).
// Exports an empty Hono instance so index.tsx compiles.
// ============================================================
import { Hono } from "npm:hono";

const ai = new Hono();

// Placeholder — Dev 6 will implement:
// POST /ai/generate
// POST /ai/approve
// POST /ai/chat
// POST /ai/keyword-popup

export default ai;
