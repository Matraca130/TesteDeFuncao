// ============================================================
// Axon v4.2 — Content CRUD Routes (Dev 1 stub)
// ============================================================
// Placeholder until Dev 1 delivers full content routes (~47).
// Exports an empty Hono instance so index.tsx compiles.
// ============================================================
import { Hono } from "npm:hono";

const content = new Hono();

// Placeholder — Dev 1 will implement full CRUD:
// institutions, courses, semesters, sections, topics,
// summaries, chunks, keywords, subtopics, connections,
// flashcards, quiz questions, ai-drafts

export default content;
