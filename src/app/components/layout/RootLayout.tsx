// ============================================================
// Axon v4.4 — Root Layout
// Top-level layout wrapper with Outlet for nested routes
// ============================================================
import { Outlet } from 'react-router';

export function RootLayout() {
  return <Outlet />;
}
